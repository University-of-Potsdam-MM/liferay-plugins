<?xml version="1.0" encoding="UTF-8"?>
<custom-sql>
    <sql id="findByUserIdAndActivityTypes">
        SELECT 
        	SocialActivitySet.*
        FROM 
        	SocialActivitySet
        INNER JOIN
            SocialActivity ON SocialActivitySet.activitysetid = SocialActivity.activitysetid
        WHERE
        	((SocialActivity.userid = ?) OR
        	(SocialActivity.receiveruserid = ?)) AND
        	(SocialActivity.type_ IN {0})
		ORDER BY
			modifiedDate DESC
    </sql>
       <sql id="countByUserIdAndActivityTypes">
        SELECT 
        	COUNT(socialActivitySet) 
        FROM 
        	SocialActivitySet socialActivitySet
        INNER JOIN
            SocialActivity ON SocialActivitySet.activitysetid = SocialActivity.activitysetid
        WHERE
        	((SocialActivity.userid = ?) OR
        	(SocialActivity.receiveruserid = ?)) AND
        	(SocialActivity.type_ IN {0})
    </sql>
    <sql id="findByUserGroupsOrUserIdAndActivityTypes">
		SELECT
			DISTINCT {SocialActivitySet.*}
		FROM
			SocialActivitySet
		WHERE
			(
				groupId IN (
					SELECT
						groupId
					FROM
						Groups_Orgs
					INNER JOIN
						Users_Orgs ON
							Users_Orgs.organizationId = Groups_Orgs.organizationId
					WHERE
						Users_Orgs.userId = ?
				) OR
				groupId IN (
					SELECT
						groupId
					FROM
						Groups_UserGroups
					INNER JOIN
						Users_UserGroups ON
							Users_UserGroups.userGroupId = Groups_UserGroups.userGroupId
					WHERE
						Users_UserGroups.userId = ?
				) OR
				groupId IN (
					SELECT
						groupId
					FROM
						Users_Groups
					WHERE
						userId = ?
				) OR
				activitysetid IN (
					SELECT
						activitysetid
					FROM
						SocialActivity
					WHERE
						((SocialActivity.userid = ?) OR
        				(SocialActivity.receiveruserid = ?)) AND
        				(SocialActivity.type_ IN {0})
				)
			)
		ORDER BY
			modifiedDate DESC
	</sql>
	
    <sql id="countByUserGroupsOrUserIdAndActivityTypes">
		SELECT 
        	COUNT(socialActivitySet) 
        FROM 
        	SocialActivitySet socialActivitySet
		WHERE
			(
				groupId IN (
					SELECT
						groupId
					FROM
						Groups_Orgs
					INNER JOIN
						Users_Orgs ON
							Users_Orgs.organizationId = Groups_Orgs.organizationId
					WHERE
						Users_Orgs.userId = ?
				) OR
				groupId IN (
					SELECT
						groupId
					FROM
						Groups_UserGroups
					INNER JOIN
						Users_UserGroups ON
							Users_UserGroups.userGroupId = Groups_UserGroups.userGroupId
					WHERE
						Users_UserGroups.userId = ?
				) OR
				groupId IN (
					SELECT
						groupId
					FROM
						Users_Groups
					WHERE
						userId = ?
				) OR
				activitysetid IN (
					SELECT
						activitysetid
					FROM
						SocialActivity
					WHERE
						((SocialActivity.userid = ?) OR
        				(SocialActivity.receiveruserid = ?)) AND
        				(SocialActivity.type_ IN {0})
				)
			)
	</sql>
    
</custom-sql>